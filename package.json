{
  "name": "site",
  "version": "1.0.10",
  "private": true,
  "scripts": {
    "build:css": "node scripts/build-site-css.mjs",
    "predev": "npm run build:css",
    "dev": "vite site --host 0.0.0.0 --port 27349",
    "test:mobile": "node --test --test-concurrency=1 tests/mobile.spec.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.1",
    "playwright": "^1.62.0",
    "vite": "^7.0.0"
  }
}
