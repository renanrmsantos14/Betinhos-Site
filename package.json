{
  "name": "site",
  "version": "1.0.10",
  "private": true,
  "scripts": {
    "dev": "vite site --host 0.0.0.0 --port 27349",
    "test:mobile": "node --test --test-concurrency=1 tests/mobile.spec.mjs"
  },
  "devDependencies": {
    "playwright": "^1.62.0",
    "vite": "^7.0.0"
  }
}
